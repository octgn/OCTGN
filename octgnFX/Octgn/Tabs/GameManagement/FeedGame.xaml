<ListBoxItem x:Class="Octgn.Tabs.GameManagement.FeedGame"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             HorizontalContentAlignment="Stretch"
             VerticalContentAlignment="Stretch" 
             d:DesignWidth="378"
             Height="Auto"
             x:Name="Me"
             
             DataContext="{Binding ElementName=Me}">
    <Grid Margin="5" Height="64">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="64"/>
            <ColumnDefinition/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Image Source="{Binding Package.IconUrl}"/>
        <StackPanel Grid.Column="1" Margin="5,0,5,0">
            <StackPanel.Resources>
                <Style TargetType="{x:Type TextBlock}">
                    <Setter Property="Margin" Value="0,3,0,0"/>
                </Style>
            </StackPanel.Resources>
            <TextBlock Text="{Binding Package.Title}" FontWeight="Bold"></TextBlock>
            <TextBlock Text="{Binding Package.Description}" TextWrapping="Wrap"></TextBlock>
        </StackPanel>
        <Grid Grid.Column="2" Width="100">
            <Grid.RowDefinitions>
                <RowDefinition Height="50*"/>
                <RowDefinition Height="50*"/>
            </Grid.RowDefinitions>
            <Button Content="{Binding InstallButtonText}"></Button>
            <Image Grid.Row="1" Source="../../Resources/Icons/Ok.png" Stretch="Uniform" Margin="5" HorizontalAlignment="Right" Visibility="{Binding Installed,Converter={StaticResource BooleanToVisibilityConverter}}"></Image>
        </Grid>
    </Grid>
</ListBoxItem>
