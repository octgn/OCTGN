<Page x:Class="Octgn.Launcher.GameList" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" mc:Ignorable="d" d:DesignHeight="300" d:DesignWidth="300"
      Title="ContactList" Loaded="PageLoaded" Unloaded="PageUnloaded">
    <Grid VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height="100*" />
            <RowDefinition Height="0" x:Name="Row2"/>
        </Grid.RowDefinitions>
        <ScrollViewer VerticalAlignment="Stretch" Style="{StaticResource ScrollPanel}">
        <StackPanel HorizontalAlignment="Stretch" Name="stackPanel1" VerticalAlignment="Top"></StackPanel>
      </ScrollViewer>
      <Button Content="Cancel" Grid.Row="2" Height="25" HorizontalAlignment="Left" Name="btnCancel" VerticalAlignment="Top" Width="75" />
    </Grid>
</Page>