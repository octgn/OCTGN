<UserControl x:Class="Octide.Views.SetSummaryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             xmlns:metro="http://schemas.codeplex.com/elysium"
             d:DesignHeight="500" d:DesignWidth="700"
             DataContext="{Binding SetSummaryViewModel, Source={StaticResource Locator}}">
    <Grid>
        <ScrollViewer Margin="10,10,0,20" VerticalScrollBarVisibility="Auto" >
            <ItemsControl Style="{StaticResource OctideInputPanel}">
                <TextBlock Text="Set Name" />
                <TextBox Text="{Binding Set.Name}" />
                <TextBlock Text="Set GUID" />
                <TextBox IsEnabled="False" Text="{Binding Set.Id, Mode=OneWay}" />
                <TextBlock Text="Hide Set from Deck Editor" />
                <StackPanel Orientation="Horizontal">
                    <metro:ToggleSwitch IsChecked="{Binding Set.Hidden, Mode=TwoWay}" Width="Auto" HorizontalAlignment="Left"/>
                    <TextBlock Text="{Binding Set.Hidden, Mode=OneWay}" FontSize="16" VerticalAlignment="Center"/>
                </StackPanel>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
